import { Routes } from '@angular/router';
import { BazasComponent } from './components/bazas/bazas.component';

export const routes: Routes = [
    {
        path: '',
        component: BazasComponent
    }
];
